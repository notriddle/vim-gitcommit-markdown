Git commit with Markdown syntax highlighting
============================================

It's just the `gitcommit.vim` syntax highlighter plugin that came with VIM, with
Markdown-specific syntax highlighting for the actual commit message.
